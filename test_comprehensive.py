import pytest\nimport sys\nimport os\nimport json\nimport numpy as np\nfrom unittest.mock import patch, MagicMock\n\n# Add parent directory to path for imports\nsys.path.insert(0, os.path.join(os.path.dirname(__file__), \"..\"))\n\nfrom backend.app import app\nimport joblib\n\n@pytest.fixture\ndef client():\n    \"\"\"Create test client for Flask app\"\"\"\n    app.config['TESTING'] = True\n    with app.test_client() as client:\n        yield client\n\n@pytest.fixture\ndef sample_prediction_data():\n    \"\"\"Sample data for prediction testing\"\"\"\n    return {\n        \"crosswalk_wait_sec\": 35.0,\n        \"litter_density_per_100m2\": 5.0,\n        \"midnight_noise_db\": 58.0,\n        \"atm_queue_length\": 2.0,\n        \"vacant_storefronts_pct\": 10.0,\n        \"dog_walk_freq_per_hr\": 1.0,\n        \"graffiti_turnover_days\": 10.0\n    }\n\nclass TestMLModel:\n    \"\"\"Test ML model functionality\"\"\"\n    \n    def test_model_exists(self):\n        \"\"\"Test that model file exists\"\"\"\n        assert os.path.exists(\"best_model.pkl\"), \"Model file not found\"\n    \n    def test_model_loading(self):\n        \"\"\"Test model can be loaded successfully\"\"\"\n        bundle = joblib.load(\"best_model.pkl\")\n        assert \"model\" in bundle\n        assert \"scaler\" in bundle\n        assert \"labels\" in bundle\n        assert \"model_name\" in bundle\n        assert \"accuracy\" in bundle\n    \n    def test_model_accuracy_threshold(self):\n        \"\"\"Test model meets minimum accuracy threshold\"\"\"\n        bundle = joblib.load(\"best_model.pkl\")\n        accuracy = bundle[\"accuracy\"]\n        assert accuracy > 0.7, f\"Model accuracy {accuracy:.4f} below threshold\"\n    \n    def test_model_prediction_format(self):\n        \"\"\"Test model prediction output format\"\"\"\n        bundle = joblib.load(\"best_model.pkl\")\n        model = bundle[\"model\"]\n        scaler = bundle[\"scaler\"]\n        \n        # Test with sample data\n        test_data = np.array([[35.0, 5.0, 58.0, 2.0, 10.0, 1.0, 10.0]])\n        scaled_data = scaler.transform(test_data)\n        prediction = model.predict(scaled_data)\n        \n        assert len(prediction) == 1\n        assert prediction[0] in [0, 1, 2], \"Invalid prediction class\"\n    \n    def test_model_prediction_consistency(self):\n        \"\"\"Test model predictions are consistent\"\"\"\n        bundle = joblib.load(\"best_model.pkl\")\n        model = bundle[\"model\"]\n        scaler = bundle[\"scaler\"]\n        \n        test_data = np.array([[35.0, 5.0, 58.0, 2.0, 10.0, 1.0, 10.0]])\n        scaled_data = scaler.transform(test_data)\n        \n        # Make multiple predictions with same data\n        pred1 = model.predict(scaled_data)[0]\n        pred2 = model.predict(scaled_data)[0]\n        pred3 = model.predict(scaled_data)[0]\n        \n        assert pred1 == pred2 == pred3, \"Model predictions inconsistent\"\n\nclass TestAPIEndpoints:\n    \"\"\"Test API endpoint functionality\"\"\"\n    \n    def test_info_endpoint(self, client):\n        \"\"\"Test /info endpoint\"\"\"\n        response = client.get('/info')\n        assert response.status_code == 200\n        \n        data = response.get_json()\n        assert 'model_name' in data\n        assert 'accuracy' in data\n        assert isinstance(data['accuracy'], (int, float))\n        assert data['accuracy'] > 0\n    \n    def test_predict_endpoint_valid_data(self, client, sample_prediction_data):\n        \"\"\"Test /predict endpoint with valid data\"\"\"\n        response = client.post('/predict', json=sample_prediction_data)\n        assert response.status_code == 200\n        \n        data = response.get_json()\n        assert 'prediction' in data\n        assert 'message' in data\n        assert data['prediction'] in ['Low', 'Med', 'High']\n        assert isinstance(data['message'], str)\n        assert len(data['message']) > 0\n    \n    def test_predict_endpoint_missing_fields(self, client):\n        \"\"\"Test /predict endpoint with missing fields\"\"\"\n        incomplete_data = {\n            \"crosswalk_wait_sec\": 35.0,\n            \"litter_density_per_100m2\": 5.0\n            # Missing other required fields\n        }\n        \n        response = client.post('/predict', json=incomplete_data)\n        assert response.status_code == 400\n        \n        data = response.get_json()\n        assert 'error' in data\n    \n    def test_predict_endpoint_invalid_data_types(self, client):\n        \"\"\"Test /predict endpoint with invalid data types\"\"\"\n        invalid_data = {\n            \"crosswalk_wait_sec\": \"invalid\",\n            \"litter_density_per_100m2\": 5.0,\n            \"midnight_noise_db\": 58.0,\n            \"atm_queue_length\": 2.0,\n            \"vacant_storefronts_pct\": 10.0,\n            \"dog_walk_freq_per_hr\": 1.0,\n            \"graffiti_turnover_days\": 10.0\n        }\n        \n        response = client.post('/predict', json=invalid_data)\n        assert response.status_code == 400\n        \n        data = response.get_json()\n        assert 'error' in data\n    \n    def test_predict_endpoint_boundary_values(self, client):\n        \"\"\"Test /predict endpoint with boundary values\"\"\"\n        boundary_data = {\n            \"crosswalk_wait_sec\": 0.0,\n            \"litter_density_per_100m2\": 0.0,\n            \"midnight_noise_db\": 30.0,\n            \"atm_queue_length\": 0.0,\n            \"vacant_storefronts_pct\": 0.0,\n            \"dog_walk_freq_per_hr\": 0.0,\n            \"graffiti_turnover_days\": 0.0\n        }\n        \n        response = client.post('/predict', json=boundary_data)\n        assert response.status_code == 200\n        \n        data = response.get_json()\n        assert 'prediction' in data\n        assert data['prediction'] in ['Low', 'Med', 'High']\n    \n    def test_predict_endpoint_high_stress_scenario(self, client):\n        \"\"\"Test prediction with high stress indicators\"\"\"\n        high_stress_data = {\n            \"crosswalk_wait_sec\": 120.0,\n            \"litter_density_per_100m2\": 25.0,\n            \"midnight_noise_db\": 90.0,\n            \"atm_queue_length\": 10.0,\n            \"vacant_storefronts_pct\": 50.0,\n            \"dog_walk_freq_per_hr\": 0.0,\n            \"graffiti_turnover_days\": 60.0\n        }\n        \n        response = client.post('/predict', json=high_stress_data)\n        assert response.status_code == 200\n        \n        data = response.get_json()\n        assert 'prediction' in data\n        # High stress scenario should likely predict High or Med stress\n        assert data['prediction'] in ['Med', 'High']\n\nclass TestAPIPerformance:\n    \"\"\"Test API performance characteristics\"\"\"\n    \n    def test_prediction_response_time(self, client, sample_prediction_data):\n        \"\"\"Test prediction response time is reasonable\"\"\"\n        import time\n        \n        start_time = time.time()\n        response = client.post('/predict', json=sample_prediction_data)\n        end_time = time.time()\n        \n        response_time = end_time - start_time\n        assert response.status_code == 200\n        assert response_time < 1.0, f\"Response time {response_time:.3f}s too slow\"\n    \n    def test_concurrent_predictions(self, client, sample_prediction_data):\n        \"\"\"Test handling multiple concurrent predictions\"\"\"\n        import threading\n        import time\n        \n        results = []\n        \n        def make_prediction():\n            response = client.post('/predict', json=sample_prediction_data)\n            results.append(response.status_code)\n        \n        # Create multiple threads\n        threads = []\n        for _ in range(10):\n            thread = threading.Thread(target=make_prediction)\n            threads.append(thread)\n        \n        # Start all threads\n        start_time = time.time()\n        for thread in threads:\n            thread.start()\n        \n        # Wait for all threads to complete\n        for thread in threads:\n            thread.join()\n        \n        end_time = time.time()\n        \n        # All requests should succeed\n        assert all(status == 200 for status in results)\n        assert len(results) == 10\n        \n        # Total time should be reasonable\n        total_time = end_time - start_time\n        assert total_time < 5.0, f\"Concurrent requests took {total_time:.3f}s\"\n\nclass TestDataValidation:\n    \"\"\"Test data validation and edge cases\"\"\"\n    \n    def test_empty_request(self, client):\n        \"\"\"Test handling of empty request\"\"\"\n        response = client.post('/predict', json={})\n        assert response.status_code == 400\n    \n    def test_null_values(self, client):\n        \"\"\"Test handling of null values\"\"\"\n        null_data = {\n            \"crosswalk_wait_sec\": None,\n            \"litter_density_per_100m2\": 5.0,\n            \"midnight_noise_db\": 58.0,\n            \"atm_queue_length\": 2.0,\n            \"vacant_storefronts_pct\": 10.0,\n            \"dog_walk_freq_per_hr\": 1.0,\n            \"graffiti_turnover_days\": 10.0\n        }\n        \n        response = client.post('/predict', json=null_data)\n        assert response.status_code == 400\n    \n    def test_negative_values(self, client):\n        \"\"\"Test handling of negative values\"\"\"\n        negative_data = {\n            \"crosswalk_wait_sec\": -10.0,\n            \"litter_density_per_100m2\": 5.0,\n            \"midnight_noise_db\": 58.0,\n            \"atm_queue_length\": 2.0,\n            \"vacant_storefronts_pct\": 10.0,\n            \"dog_walk_freq_per_hr\": 1.0,\n            \"graffiti_turnover_days\": 10.0\n        }\n        \n        response = client.post('/predict', json=negative_data)\n        # API should handle negative values gracefully\n        assert response.status_code in [200, 400]\n\nif __name__ == \"__main__\":\n    pytest.main([__file__, \"-v\"])